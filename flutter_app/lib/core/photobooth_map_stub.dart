import 'package:flutter/material.dart';

class PhotoboothMap extends StatelessWidget {
  const PhotoboothMap({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Map preview is available on the web app.'));
}
